export const currentContacts = state => state.resources.contacts;
export const isGenerating = state => state.resources.generating;
export const selectedProvider = state => state.resources.provider;
export const isAuthenticated = state => state.isAuthenticated;
export const profile = state => state.context.profile;
export const jwtToken = state => state.context.jwtToken;
